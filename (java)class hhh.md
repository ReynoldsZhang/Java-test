package test;

public class hhh {
    public static void main(String[] args){

        son side = new son();
        //App p=new App();
        double sx = Math.random()*5;//Math.random()*5会随机产生零到五区间内的小数
        int wdnmd = (int)(Math.random()*4+2);//它能取区间0到5的整数，且可取区间左边的临界值，不可取区间右边的临界值
        int op = 12;
        Integer app = new Integer(5);//Integer 是java自带的包装类。
        Double aqq = new Double(13.14);
        String pqq = new String("90mn90hoioi");

       // father si =new father(3,9);

        //side.mmm();
        //side.hH();
        //side.father();//在对象内部，双等号是用来判断两个对象内存地址是否一样。.equals也是用来判断内存地址是否一样
        //side.mHe();
        //side.mme();

        /*
        System.out.println(app.doubleValue());//doubleValue为Integer里面自带的返回值的自带double类型方法。
        System.out.println(app.MAX_VALUE);
        System.out.println(app.MIN_VALUE);
        System.out.println(pqq.length());//此为输出字符串的长度
        System.out.println(pqq);//直接输出赋值
        System.out.println(pqq.substring(2,5));//。substring的方法为a到b区间内的值[a，b）
        System.out.println(pqq.substring(2));//这里输出从2区间开始往后的值
        System.out.println(pqq.substring(2,10));//不能输出，超过区间的数
        System.out.println(pqq.substring(5,8));
        System.out.println(pqq.substring(4,5));
        System.out.println(pqq.substring(4,4));
        System.out.println(pqq.indexOf("90"));//它的作用适用于查找某一个自负或者字符串，在里面首次出现的位置
        System.out.println("9090".compareTo("9090"));//字符串比较大小
        System.out.println("9090".compareTo("oioi"));
        System.out.println("adm".compareTo("abc"));
        System.out.println("aaaaaa".compareTo("aaa"));
        */

        //System.out.println(side.high);
        // System.out.println(side.ez);
        //System.out.println(3+app);
        //System.out.println(aqq.intValue());//并非四舍五入，只是取整数部分
        //System.out.println(sx);
        //System.out.println(wdnmd);
        //System.out.println(Math.abs(-1234));//绝对值
        //System.out.println(Math.ceil(12.53));//向上取整，取大于这个数字的整数
        //System.out.println(Math.floor(-383.9999));//向下取整， 取小于这个数子的整数
        //System.out.println(Math.round(123.999));//四舍五入
        //System.out.println(Math.pow(3.1415927,3.1415926));//计算次方3.1415927为底数，3.1415926为指数
        //System.out.println(Math.sqrt(343213.2321232));//开方
    }
}
